package org.vgk.hr.db.entity;

/**
 * Откуда берётся значение поля при генерации.
 */
public enum FieldValueSource {
    /** Значение вводит пользователь (или передаётся в API генерации). */
    USER,
    /** Значение вычисляется системой (например, текущая дата). */
    SYSTEM
}
